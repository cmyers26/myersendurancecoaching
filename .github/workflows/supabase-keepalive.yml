name: Supabase Keep Alive

on:
  schedule:
    - cron: '0 12 * * *' # once per day (12:00 UTC)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -X GET "https://www.myersendurancecoaching.com/api/keep-alive"
